## 2026-06-21T09:33:57.0569980-07:00 - codex/uiux-extension-fix-guide-0932 - Mode 1 side-panel fix guide
- **What changed:** Added a standalone Mode 1 extension-experience prototype with scan-direction cards, a pinned side panel, a fixability rail, dimension accordions, severity chips, visible `Confidence TODO` chips, step-by-step live-site remediation, step checkoffs, command-menu shortcuts, and verify/highlight TODO states. The header includes a grade ring, but it says `TBD / TODO grade` because no grade field is exposed.
- **Owned paths touched:** `ui-next/styles.css`, `extension/index.html`, `extension/app.js`, `design/data/extension-fix-guide-0932-summary.json`, `design/reference-extension-fix-guide-0932.md`, `design/reference/extension-fix-guide-0932/*.png`, `design/shots/extension-fix-guide-0932/{080,100,125,150}.png`, `desktop/.gitkeep`, `docs/handoff-uiux.md`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` for `project_id`, `run_id`, `live_scan.run_id`, `risk_map.findings`, finding `severity`, finding `category`, and selected `verify.*` booleans. Browser confidence, A-F grade, DOM selectors, request URLs, canary in-out traces, and scan mode are absent from the endpoint and are labeled `TODO`.
- **How verified:** Ran a temporary Node/Playwright static server that mapped only `extension/`, `ui-next/`, and `design/` on a random non-7878 port. Tested scan mode switching, dimension filtering, accordion collapse/expand, remediation step checkoff, highlight TODO, verify TODO, command menu, and keyboard dimension shortcut. Asserted no horizontal overflow at 80/100/125/150; no console errors.
- **Screenshots:** `design/shots/extension-fix-guide-0932/{080,100,125,150}.png`.
- **Proposal for live surfaces (if any):** none.
- **Blockers / open items:** `/api/state` does not expose Mode 1 browser evidence, confidence, A-F grade, DOM selectors, request URLs, or canary traces, so the prototype keeps those as visible TODO states rather than inventing them.
- **Next suggested increment:** If Claude exposes browser evidence fields, replace `Confidence TODO`, `Highlight TODO`, and `TBD` grade with real values and selectors.
