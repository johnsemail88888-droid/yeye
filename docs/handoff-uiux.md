## 2026-06-21T09:48:31.3951531-07:00 - codex/uiux-desktop-project-intake-0947 - Mode 2 project intake isolation chooser
- **What changed:** Added a standalone Mode 2 desktop intake prototype that lets the user choose `Sandbox copy` or `Working copy`, pick a source type, add the project locally, see the project row activate, and read the next scan timeline. It uses real `/api/state` project and risk summary fields while marking folder picker, clone, zip upload, and working-copy writes as `MOCK TODO`.
- **Owned paths touched:** `ui-next/styles.css`, `desktop/index.html`, `desktop/app.js`, `design/data/desktop-project-intake-0947-summary.json`, `design/reference-desktop-project-intake-0947.md`, `design/reference/desktop-project-intake-0947/*.png`, `design/shots/desktop-project-intake-0947/{080,100,125,150}.png`, `extension/.gitkeep`, `docs/handoff-uiux.md`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` for `project_id`, `run_id`, `live_scan.run_id`, `risk_map.findings.length`, `risk_map.scanned_files`, `risk_map.high_impact_tools.length`, `risk_map.guard_present`, `risk_map.approval_gate_present`, and selected `verify.*` booleans. Real project upload, clone, zip import, folder picker, write mode, and file spans are absent from the endpoint and are labeled `MOCK TODO`.
- **How verified:** Ran a temporary Node/Playwright static server that mapped only `desktop/`, `ui-next/`, and `design/` on a random non-7878 port. Tested Working copy selection, Git URL source selection, local add-project state, command menu, `Ctrl+1` Sandbox shortcut, and `Ctrl+2` Working shortcut. Asserted no horizontal overflow at 80/100/125/150; no console errors.
- **Screenshots:** `design/shots/desktop-project-intake-0947/{080,100,125,150}.png`.
- **Proposal for live surfaces (if any):** none.
- **Blockers / open items:** `/api/state` does not expose a non-mutating real project intake surface, so the import path is a local UI-only `MOCK TODO` flow.
- **Next suggested increment:** Add a real read-only artifact for project intake status if Claude exposes it, then replace the mocked import states with real folder/clone/upload metadata.
