# UI/UX Handoff Log

## 2026-06-21T07:16:57-07:00 - codex/uiux-extension-scan-history - Mode 1 scan history strip

- **What changed:** Built a standalone Mode 1 extension-style prototype with a compact recent-scan history strip above the 3 scan choices. The current history row records selected scan type, acknowledgement state, and the latest daemon-backed result summary. Placeholder history rows are visibly labeled `MOCK TODO` and can be hidden with a local clear action.
- **Owned paths touched:** `ui-next/styles.css`; `extension/index.html`; `extension/app.js`; `extension/favicon.svg`; `extension/state-summary.json`; `desktop/.gitkeep`; `design/reference-extension-scan-history.md`; `design/data/extension-scan-history-state-summary.json`; `design/reference/extension-scan-history/{linear-changelog,raycast-store,raycast-extensions}.png`; `design/shots/extension-scan-history/{080,100,125,150}.png`; `docs/handoff-uiux.md`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` was available. I wrote a minimized, non-PII summary to `design/data/extension-scan-history-state-summary.json` and copied the same summary into `extension/state-summary.json` for offline prototype binding. Displayed counts are from that summary: `finding_count=8`, `critical_count=3`, `high_count=3`, `before_fail_count=5`, `after_fail_count=0`, `demo_ready=true`, `report_audit_pass=true`. Placeholder history slots are labeled `MOCK TODO`.
- **How verified:** Served only owned roots (`extension/` and `ui-next/`) on a temporary local port, not `7878`. Did not start the daemon and did not run project `npm`, `tsx`, or `demo:*` commands. Interaction checks: loaded real summary count `8`; Active Probe disabled before consent and enabled after consent; clicking Active Probe updates the current history row; clearing history hides only `MOCK TODO` slots; Deep Scan can be reselected. Console errors: `0`. Zoom-equivalent checks passed at 80/100/125/150 with no horizontal overflow, no clipped control/card text, min tap targets OK, and popup/side panel non-overlap.
- **Screenshots:** `design/shots/extension-scan-history/{080,100,125,150}.png`
- **Reference study:** Captured Linear changelog and Raycast Store into `design/reference/extension-scan-history/`. Used Linear's quiet dark timeline and metadata rhythm for the recent-scan strip. `raycast-extensions.png` is a kept access note only because that URL returned a 404 page.
- **Proposal for live surfaces (if any):** none. The prototype proves the interaction; live adoption should add a small scan-history store keyed by real `run_id` or scan artifact path before any live surface renders history.
- **Blockers / open items:** No blocker. One verification issue was found and fixed: container queries originally targeted the grid container itself, so 150 percent width could visually overlap; `body` now owns the query container and the 150 percent screenshot is clean.
- **Next suggested increment:** Add the Mode 1 "Send to Phoenix / export" action strip with honest `local fallback` status unless a real Phoenix artifact is present.
