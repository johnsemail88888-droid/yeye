## 2026-06-21T09:19:07.9480476-07:00 - codex/uiux-desktop-preview-refresh - Mode 2 preview refresh lane
- **What changed:** Added a standalone desktop Mode 2 prototype that keeps the Linear/Codex 3-pane shell, lets a user open a pending finding hunk, accept or reject it, and then refreshes a before/after preview lane for the selected finding. The preview clearly distinguishes real `/api/state` findings from `MOCK TODO` source spans, patch hunks, and rendered previews.
- **Owned paths touched:** `ui-next/styles.css`, `desktop/index.html`, `desktop/app.js`, `design/data/desktop-preview-refresh-summary.json`, `design/reference-desktop-preview-refresh.md`, `design/reference/desktop-preview-refresh/*.png`, `design/shots/desktop-preview-refresh/{080,100,125,150}.png`, `extension/.gitkeep`, `docs/handoff-uiux.md`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` for `project_id`, `run_id`, `live_scan.run_id`, `risk_map.findings`, `risk_map.guard_present`, `risk_map.approval_gate_present`, and selected `verify.*` booleans. File spans, patch hunks, and rendered preview states are absent from the endpoint and are labeled `MOCK TODO`.
- **How verified:** Ran a temporary Node/Playwright static server that mapped only `desktop/`, `ui-next/`, and `design/` on a random non-7878 port. Tested selecting F2, opening a pending hunk, accepting via `Tab` and seeing the preview refresh, rejecting F3 via `Esc`, accepting all visible via `Ctrl+Enter`, toggling the command menu via `Ctrl+K`, and asserting no horizontal overflow at 80/100/125/150. No console errors.
- **Screenshots:** `design/shots/desktop-preview-refresh/{080,100,125,150}.png`.
- **Proposal for live surfaces (if any):** none.
- **Blockers / open items:** Real source coordinates, patch text, and re-rendered application preview are not exposed by `/api/state`; this prototype keeps them as local `MOCK TODO` surfaces rather than inventing them.
- **Next suggested increment:** Bind a real artifact for file span coordinates or patch hunk text if Claude exposes one, then replace the local `MOCK TODO` diff lines.
