## 2026-06-21T10:03:39.4462790-07:00 - codex/uiux-extension-export-packet-1002 - Mode 1 local export packet preview
- **What changed:** Added a standalone Mode 1 extension export-packet prototype with scan framing cards, finding include/exclude toggles, severity filters, evidence reference rows, a JSON packet preview, command-menu actions, and a Phoenix destination fallback that is clearly labeled `TODO local fallback` and makes no network call.
- **Owned paths touched:** `ui-next/styles.css`, `extension/index.html`, `extension/app.js`, `design/data/extension-export-packet-1002-summary.json`, `design/reference-extension-export-packet-1002.md`, `design/reference/extension-export-packet-1002/*.png`, `design/shots/extension-export-packet-1002/{080,100,125,150}.png`, `desktop/.gitkeep`, `docs/handoff-uiux.md`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` for `project_id`, `run_id`, `live_scan.run_id`, `risk_map.findings`, finding severities, selected `verify.*` booleans, and `verify.evidence_files`. Phoenix export status, browser confidence, DOM selectors, request URLs, and canary traces are absent from the endpoint and are labeled `TODO`.
- **How verified:** Ran a temporary Node/Playwright static server that mapped only `extension/`, `ui-next/`, and `design/` on a random non-7878 port. Tested Active Probe framing, finding include/exclude, Critical filter, include visible, Phoenix fallback preview, command menu, clear packet, and restore all. Asserted no horizontal overflow at 80/100/125/150; no console errors.
- **Screenshots:** `design/shots/extension-export-packet-1002/{080,100,125,150}.png`.
- **Proposal for live surfaces (if any):** none.
- **Blockers / open items:** `/api/state` does not expose a real Phoenix export artifact or browser evidence fields, so export remains a local UI-only packet preview.
- **Next suggested increment:** Bind real browser evidence fields if Claude exposes them, then replace the TODO confidence/evidence rows in the packet preview.
