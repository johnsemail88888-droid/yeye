## 2026-06-21T09:03:31.3416767-07:00 - codex/uiux-desktop-apply-preview - Mode 2 gated apply preview
- **What changed:** Built a standalone Mode 2 prototype for the Apply gate. Selecting a live finding opens an inline red/green pending hunk in the editor; `Tab` accepts the hunk into local UI state, `Esc` rejects it, and `Cmd/Ctrl+Enter` accepts all currently visible pending hunks as local prototype state. No source files are written.
- **Owned paths touched:** `ui-next/styles.css`, `desktop/index.html`, `desktop/app.js`, `design/data/desktop-apply-preview-summary.json`, `design/reference/desktop-apply-preview/*.png`, `design/reference-desktop-apply-preview.md`, `design/shots/desktop-apply-preview/{080,100,125,150}.png`, `docs/handoff-uiux.md`, `extension/.gitkeep`.
- **Data source:** Real read-only `GET http://127.0.0.1:7878/api/state` snapshot captured into `design/data/desktop-apply-preview-summary.json`. Used `project_id`, `run_id`, `live_scan.run_id`, `verify.demo_ready`, `verify.report_audit_pass`, severity counts, and `risk_map.findings[].{id,tool,category,severity,detail}`. Real file spans and patch hunks are absent, so inline hunk text is visibly labeled `MOCK TODO`.
- **How verified:** Served only owned dirs with a temporary local static server, opened `desktop/index.html`, selected findings, opened pending hunk, accepted with `Tab`, rejected with `Esc`, accepted all with `Cmd/Ctrl+Enter`, and captured zoom-equivalent 80/100/125/150 screenshots. No horizontal overflow or console errors were observed.
- **Screenshots:** `design/shots/desktop-apply-preview/{080,100,125,150}.png`
- **Proposal for live surfaces (if any):** none
- **Blockers / open items:** `/api/state` lacks exact Mode 2 file paths, line spans, dimensions, and real patch hunks; all patch text stays `MOCK TODO`.
- **Next suggested increment:** Add a Mode 2 preview refresh lane that shows a before/after rendered component state after accepting a hunk, still without writing source.
