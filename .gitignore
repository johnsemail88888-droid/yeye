# deps
node_modules/

# local secrets (never commit)
.env
.env.local
.env.*.local

# logs
*.log

# generated artifacts (regenerated by the loop / CI — not source)
runs/
.vibeshield/
verify_results.json
risk_map.json
dataset.json
