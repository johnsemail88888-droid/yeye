# VibeShield Claude Code Launcher

Paste this into Claude Code after placing `VIBESHIELD_STUDIO_MASTER_PROMPT_V4_RESEARCH_LOOPS.md` at the repository root:

```text
Read VIBESHIELD_STUDIO_MASTER_PROMPT_V4_RESEARCH_LOOPS.md completely and treat it as the controlling specification.

Do not summarize it and do not stop after planning. Inspect the existing repository, preserve working code, run the current build/tests, write CURRENT_STATE.md and IMPLEMENTATION_PLAN.md from verified facts, then immediately continue the P0 implementation.

The non-negotiable path is: cached sourced risk corpus → authorized selected-scope scan → real BEFORE failures → Phoenix/local trace and evidence → actual source/runtime fix with ArmorIQ or a correctly labeled compatible guard → identical attack + mutation + benign AFTER suite → utility preserved → audited developer/founder reports → connected desktop/browser/VS Code surfaces → CI artifacts.

Use deterministic scanners before LLM judgment. Never invent a metric, source, legal conclusion, integration, or blocked action. Resume from workflow_state.json after interruption. Ask me only for unavoidable OS permission, target authorization, or a credential to place locally in .env.local. Continue until pnpm verify has run and verify_results.json contains overall_pass=true with evidence paths.
```
